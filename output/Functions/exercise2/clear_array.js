// Generated by CoffeeScript 2.0.3
(function() {
  // (1)
  var clearArray, run, what, xInContext;

  clearArray = function(arr) {
    arr.splice(0, arr.length);
  };

  // (2)
  // arr
  run = function(func, ...args) {
    return func.apply(this, ...args);
  };

  if (typeof warning !== "undefined" && warning !== null) {
    // (3)
    return abortMission(warning);
  }

  if (typeof warning !== "undefined" && warning !== null) {
    return abortMission(warning);
  }

  if (typeof warning !== "undefined" && warning !== null) {
    return abortMission(warning);
  }

  if (typeof warning !== "undefined" && warning !== null) {
    return abortMission(warning);
  }

  return abortMission(typeof warning !== "undefined" && warning !== null ? warning : void 0);

  // （6）
  xInContext = function() {
    return console.log(this.x);
  };

  what = {
    x: 'quantum entanglement'
  };

  xInContext.call(what);

}).call(this);
