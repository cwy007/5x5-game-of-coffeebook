// Generated by CoffeeScript 2.0.3
(function() {
  var animate, arr, decimate, fn, i, j, len, obj, objj, x;

  obj = new Object();

  objj = {};

  arr = [];

  arr[{
    toString: function() {
      return '1';
    }
  }];

  console.log(((function() {
    var j, results;
    results = [];
    for (i = j = 1; j <= 3; i = ++j) {
      results.push('practice');
    }
    return results;
  })()).join(', '));

  fn = function(x) {
    return setTimeout((function() {
      return console.log(x);
    }), 0);
  };
  for (j = 0, len = arr.length; j < len; j++) {
    x = arr[j];
    fn(x);
  }

  decimate = function(army) {
    var k, len1, results, soldier;
    results = [];
    for (k = 0, len1 = army.length; k < len1; k += 10) {
      soldier = army[k];
      results.push(execute(soldier));
    }
    return results;
  };

  animate = function(startTime, endTime, framesPerSecond) {
    var k, pos, ref, ref1, ref2, results;
    results = [];
    for (pos = k = ref = startTime, ref1 = endTime, ref2 = 1 / framesPerSecond; ref2 > 0 ? k <= ref1 : k >= ref1; pos = k += ref2) {
      results.push(addFrame(pos));
    }
    return results;
  };

}).call(this);
