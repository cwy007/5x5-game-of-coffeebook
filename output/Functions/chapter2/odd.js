// Generated by CoffeeScript 2.0.3
(function() {
  var count, e, incrementCount, odd;

  odd = function(num) {
    if (typeof num === 'number') {
      if (num === Math.round(num)) {
        if (num > 0) {
          return num % 2 === 1;
        } else {
          throw `${num} is not positive`;
        }
      } else {
        throw `${num} is not an integer`;
      }
    } else {
      throw `${num} is not an integer`;
    }
  };

  try {
    odd(5.1);
  } catch (error) {
    e = error;
    console.log(e);
  }

  count = 0;

  incrementCount = function() {
    return count++;
  };

  incrementCount();

}).call(this);
