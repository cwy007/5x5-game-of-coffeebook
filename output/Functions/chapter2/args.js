// Generated by CoffeeScript 2.0.3
(function() {
  var callback, refine, ringFireAlarm, sandwich, setName1, setName2,
    slice = [].slice;

  setName1 = function(name) {
    return this.name = name;
  };

  setName2 = function(name1) { // no code required!
    this.name = name1;
  };

  callback = (message) => {
    return this.voicemail.push(message);
  };

  ringFireAlarm = function(isDrill = true) {};

  // it's pretty much always a drill
  // isDrill = true unless isDrill?
  // isDrill ?= true
  refine = function(wheat, ...chaff) {
    console.log(`The best: ${wheat}`);
    return console.log(`The rest: ${chaff.join(', ')}`);
  };

  refine('great', 'not bad', 'so-so', 'meh');

  sandwich = function(beginning, ...middle) {
    var end, i, ref;
    ref = middle, middle = 2 <= ref.length ? slice.call(ref, 0, i = ref.length - 1) : (i = 0, []), end = ref[i++];
  };

  // ...
  stdin.on('data', function(input) {});

}).call(this);
